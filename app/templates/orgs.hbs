<h1>Orgs Listing</h1>
<ul>
  {{#each model as |org|}}
  <li>
    <span {{action 'addToFavorites' org}}>
      [Favorite!]
    </span>
    {{link-to org.id 'org' org.id }}
  </li>
  {{else}}
  <li>Sorry the list is empty</li>
  {{/each}}
</ul>
{{outlet}}
